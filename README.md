# F0 (fishtank) streaming setup

raspberry pi with hdmi-csi capture card as a video source > ffmpeg > `ownstream` live video streaming platform.

requires two machines:
 - owncast + ffmpeg 
 - rpi with csi-hdmi

read instructions in subdirectories.


